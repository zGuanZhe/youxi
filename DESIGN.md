# Amethyst Gaze · 设计说明

## 立意

紫晶凝视——紫色主导的精英凝视感，表面优雅深处浓烈。
参考角色出自《欢迎来到实力至上主义的教室》，带病娇气质；用户喜欢紫色。
转译为皮肤语言：精英感 = 编辑式排版 + 金箔描边 + 衬线标题；
病娇感 = 紫晶凝视的呼吸点 + 玫瑰暖色暗流 + 苔青冷色对位；
"被凝视"的氛围由 5 个分布在 5 个组件上的签名动效共同织出。

## 第二层 craft（用户的核心要求）

> "第二层应该是为每个组件单独设计，有专属图案，有不同的动态特效，
> 背景也让人有沉浸感，设计完成度要高。"

每组件都有独立造型 + 独立签名动效：

| 组件 | 专属图案 | 签名动效 |
|---|---|---|
| 侧栏 | 木纹渐变 + 金箔右沿 + 切面紫晶 brand mark | 活跃项 3.2s 光晕呼吸 |
| 顶栏 | 阅读灯暖晕 + 底部金线 + 苔青状态药丸 | 状态点 2.4s 脉冲 |
| 标题牌 | 编辑式牌匾（左厚金边）+ 玻璃 + 凝视 radial | 右上 8px 紫晶 4.2s 呼吸 |
| 建议卡 | 四色语义（紫/玫/青/金）+ 各自 SVG | 图标 hover 4s 微缩放 |
| 输入框 | 缎带顶 + 紫雾玻璃 + focus 扩散环 | focus 态 ring 扩散 |
| 背景 | 参考图 + 双向暖紫纱 + radial 暖点 + vignette | 24s 极慢漂移 |

## 双轨技术模型

- Track 1 `theme.css`（沙箱安全）：底色/边框/阴影/玻璃模糊/过渡。通过 `safe-css-validator` 校验，22 规则 95 声明 4396 字节。不含 animation/transform/background-image/伪元素。
- Track 2 `amethyst-gaze-advanced.css`（gated base）：所有渐变/动效/图案/伪元素。由 `html[data-dream-theme-id="preset-amethyst-gaze"]` 严格门控，只在本主题激活时生效。

这套模型来自 Orchid Concourse 范本：把不可沙箱化的视觉放在系统级 base CSS 的主题命名空间下，保持 theme.css 本身的安全可加载。

## 调色板逻辑

深茄紫底（#14101c）做"浓"——给长时间编码的眼睛低疲劳的暗面；
紫水晶 accent（#b079e0）做"亮"——是焦点也是呼吸点的颜色；
玫瑰暖色（#d98fb0）做"暖暗流"——藏在标题牌渐变末段，给冷紫底一丝体温；
苔青冷色（#6ba89a）做"冷对位"——只在状态点和次级语义卡出现，平衡紫的偏色；
金箔（#c9a857）做" prestige trim"——描边、活跃指示条、缎带，给精英感加金属质感。
亮色模式不是反相，是同色系的淡紫雾化（#f3e9f7 / #7b3fb0），保持识别度。

## 完成度自检

- token 合同：每个颜色同时有 hex + RGB triple，支持 `rgb(var(--*-rgb) / .xx)` 色彩混合
- 明暗双模式：所有渐变面都有亮色变体
- 无障碍：`prefers-reduced-motion` 关闭全部 5 个动效
- 响应式：880px 以下侧栏横向折叠、卡片单列
- 沙箱安全：theme.css 通过 safe-css-validator
- 应用安全：高阶 CSS 全部由 theme ID 门控，不影响其他主题
